language: cpp
compiler: gcc

dist: bionic

addons:
  apt:
    packages:
      - gcc-7
      - g++-7

script:
  - cmake --version
  - rm -rf ./build
  - mkdir build
  - cd build
  - cmake ..
  - ls
  - make
  - ls
  - example/EnlivengineExample
  - tests/EnlivengineTests
  - cd ..
  - rm -rf ./build
